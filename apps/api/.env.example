printf "%s\n" '# Which adapters to use, comma-separated
ADAPTERS=mock
# Public API base (used by web app, docs, etc.)
NEXT_PUBLIC_API=http://localhost:8000
# Review connectors (optional for later)
GOOGLE_LOCATION_ID=
IMAP_URL=
IMAP_USER=
IMAP_PASS=' > apps/api/.env.example

git add apps/api/.env.example
git commit -m "Task: config ; add API .env.example with adapter flags"